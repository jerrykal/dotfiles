type -q bat; and alias cat="bat"
type -q batcat; and alias cat="batcat"; and alias bat="batcat"

type -q autossh; and alias ssh="autossh -M 0"

type -q mise; and alias mise-update="mise self-update -y && mise up"

alias mkdir="mkdir -p"

alias t="~/.tmux/scripts/session-picker.sh"
