- My interactive shell is fish.
- I manage tool versions and environments with mise.
- When porting code from another local repo, write commit messages, PR text and docstrings in the target repo's own terms. The target's readers cannot see the source repo; cite it only where the target already does.
- Use worktrunk to manage git worktrees.

@~/.claude/CLAUDE.local.md
