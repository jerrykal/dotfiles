- My interactive shell is fish.
- When reporting information to me, be extremely concise and sacrifice grammar for the sake of concision.
- Don't retro-add docstrings, comments, or type annotations to code you aren't otherwise changing.
- When designing frontend, always prefer dark theme over light theme unless user stated otherwise.
- Prefer the `LSP` tool over `Grep`/`Glob` for semantic code queries where a language server is available; use `Grep`/`Glob` for plain-text search, non-code files, or when no LSP server is running.

@~/.claude/CLAUDE.local.md
