# --- General ---
# set prefix to ctrl+space
unbind C-b
set-option -g prefix C-Space
bind C-Space send-prefix

# Enable mouse support
set -g mouse on

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Renumber windows on window close
set -g renumber-windows on

# When the current session is destroyed, the client is detached iff there are no detached session
set -g detach-on-destroy no-detached

# Allow passthrough for OSC52 clipboard sequences
set -g set-clipboard on
set -g allow-passthrough all

# Enable extended keys support
set -g extended-keys on
set -g extended-keys-format csi-u

set -g focus-events on

# `notify` writes a BEL into the triggering pane so monitor-bell flags that
# window in the status line. The default bell-action `any` would also ring the
# current window, defeating notify's "stay quiet where the user is watching".
set -g bell-action other

# --- Terminal & display ---
# Setup true-colors
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",${TERM}:Tc"
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'  # undercurl support
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'  # underscore colours - needs tmux-3.0

# Blinking block cursor (tmux 3.5+)
set -g cursor-style blinking-block

# Set terminal title to "hostname: session"
set -g set-titles on
set -g set-titles-string "#h: #S"

# Set main-pane height and width
set -g main-pane-height 75%
set -g main-pane-width 70%

# --- Copy mode ---
# Vim copy mode
setw -g mode-keys vi
bind P paste-buffer
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-selection

# C-v with a selection up just switches it between block and line; only a
# fresh one needs begin-selection, which would otherwise collapse the
# selection to the cursor. selection_active, not selection_present, since the
# latter is still 0 while a one-character selection is being dragged out.
bind -T copy-mode-vi C-v {
  send -X rectangle-toggle
  if -F "#{!=:#{selection_active},1}" { send -X begin-selection }
}

# Y yanks like y, but puts the pane and the scrollback lines the text came from
# in front of it, so pasting into an agent is enough for the agent to pull the
# surrounding output back with `tmux-ref show`. No selection takes the cursor
# line — keyed off selection_active, since selection_present is still 0 while a
# fresh selection is only one character wide. Only `pipe` leaves the buffer
# alone; the copy-pipe variants would overwrite tmux-ref's annotated one with
# the bare selection.
bind -T copy-mode-vi Y {
  if -F "#{!=:#{selection_active},1}" { send -X select-line }
  send -X pipe "~/.local/bin/tmux-ref yank '#{pane_id}' '#{selection_start_y}' '#{selection_end_y}'"
}

# Vim-like incremental search (n/N repeat as usual)
bind -T copy-mode-vi / command-prompt -i -p "/" "send-keys -X search-forward-incremental \"%%%\""
bind -T copy-mode-vi ? command-prompt -i -p "?" "send-keys -X search-backward-incremental \"%%%\""

# Smoother mouse scroll
bind -T copy-mode-vi WheelUpPane send -N 1 -X scroll-up
bind -T copy-mode-vi WheelDownPane send -N 1 -X scroll-down
bind -T copy-mode WheelUpPane send -N 1 -X scroll-up
bind -T copy-mode WheelDownPane send -N 1 -X scroll-down

# Do not exit copy-mode after mouse selection
unbind -T copy-mode-vi MouseDragEnd1Pane
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-selection
unbind -T copy-mode MouseDragEnd1Pane
bind -T copy-mode MouseDragEnd1Pane send-keys -X copy-selection

# Right-drag selects without copying: no MouseDragEnd3Pane binding, so the
# highlight is left for a y or Y to act on, unlike a left drag. Unlike the
# left-drag bindings it never defers to the pane via mouse_any_flag -- that is
# the point of using button 3: nvim and Claude Code take the mouse for
# themselves, so only a gesture tmux keeps can select over them. MouseDown3Pane
# fires on press, before the drag is known, so the default context menu has to
# go or it would open every time; M-right-click still shows it.
unbind -T root MouseDown3Pane
bind -T root MouseDown3Pane select-pane -t =
bind -T root MouseDrag3Pane copy-mode -M
bind -T copy-mode-vi MouseDrag3Pane select-pane \; send -X rectangle-off \; send -X begin-selection
bind -T copy-mode MouseDrag3Pane select-pane \; send -X rectangle-off \; send -X begin-selection

# Ctrl held over either drag makes it rectangular. Ctrl takes the gesture for
# tmux in both cases, mouse_any_flag branch and all, so it selects over nvim
# and Claude Code the way the right drag does -- at the cost of those panes
# never seeing a Ctrl-click of their own, nvim's C-LeftMouse tag jump included.
#
# Rectangle mode has to be on before the drag starts, not after:
# window_copy_update_cursor clamps the column to the end of the line unless
# rectflag is already set, so turning it on afterwards leaves
# the block anchored at the line's end -- column 0 on a blank line. copy-mode
# -M only starts a drag when it is what enters copy-mode, so the press arms
# rectangle mode and the drag then runs through the in-mode bindings below.
unbind -T root C-MouseDown1Pane
bind -T root C-MouseDown1Pane { select-pane -t = ; copy-mode ; send -X rectangle-on }
bind -T root C-MouseDown3Pane { select-pane -t = ; copy-mode ; send -X rectangle-on }
bind -T root C-MouseDrag1Pane { copy-mode -M ; send -X rectangle-on }
bind -T root C-MouseDrag3Pane { copy-mode -M ; send -X rectangle-on }

bind -T copy-mode-vi C-MouseDrag1Pane select-pane \; send -X rectangle-on \; send -X begin-selection
bind -T copy-mode C-MouseDrag1Pane select-pane \; send -X rectangle-on \; send -X begin-selection
bind -T copy-mode-vi C-MouseDrag3Pane select-pane \; send -X rectangle-on \; send -X begin-selection
bind -T copy-mode C-MouseDrag3Pane select-pane \; send -X rectangle-on \; send -X begin-selection

bind -T copy-mode-vi C-MouseDragEnd1Pane send-keys -X copy-selection
bind -T copy-mode C-MouseDragEnd1Pane send-keys -X copy-selection

# A Ctrl-click that never became a drag would otherwise leave the pane sitting
# in the copy-mode its press opened.
bind -T copy-mode-vi C-MouseUp1Pane if -F "#{!=:#{selection_active},1}" { send -X cancel }
bind -T copy-mode C-MouseUp1Pane if -F "#{!=:#{selection_active},1}" { send -X cancel }
bind -T copy-mode-vi C-MouseUp3Pane if -F "#{!=:#{selection_active},1}" { send -X cancel }
bind -T copy-mode C-MouseUp3Pane if -F "#{!=:#{selection_active},1}" { send -X cancel }

bind -T copy-mode-vi MouseDrag1Pane select-pane \; send -X rectangle-off \; send -X begin-selection
bind -T copy-mode MouseDrag1Pane select-pane \; send -X rectangle-off \; send -X begin-selection

# --- History tracking ---
# The pickers preview by really switching to what you highlight, so they hold
# @picker_busy and write the history they skipped by hand on the way out.
# Hooks run after the command list that fired them, so a picker must set the
# flag in a call of its own for it to cover the first switch. Reloading this
# file clears it, in case a picker ever dies without clearing it itself.
set -g @picker_busy 0

# Windows, most recently used first. tmux's own stack (window_stack_index)
# can't say it: the current window sits at 0 alongside every window never
# selected. So each window carries a stamp from a global counter instead.
# Reloading this file must not rewind the counter below the stamps already
# handed out, so it only seeds an unset one.
if -F "#{==:#{@mru_seq},}" { set -g @mru_seq 0 }
set-hook -g session-window-changed[44] {
  if -F "#{!=:#{@picker_busy},1}" {
    set -gF @mru_seq "#{e|+|:#{@mru_seq},1}"
    setw -F @mru "#{@mru_seq}"
  }
}

# --- Keybindings ---
# Pane
# Jump to the last active pane, even across windows and sessions (the built-in
# last-pane is window-local). pane-focus-in fires whenever the attached
# client's active pane changes (needs focus-events on).
set-hook -g pane-focus-in[43] {
  if -F "#{&&:#{!=:#{@picker_busy},1},#{!=:#{pane_id},#{@current-pane}}}" {
    set -gF @last-pane "#{@current-pane}"
    set -gF @current-pane "#{pane_id}"
  }
}
bind \' run-shell '
  pane="#{@last-pane}"
  { [ -n "$pane" ] &&
    tmux select-pane -t "$pane" &&
    tmux select-window -t "$pane" &&
    tmux switch-client -t "$pane"
  } 2>/dev/null || tmux display-message "no last pane"
'
bind space display-panes -d 0 "swap-pane -dt %%"
bind J join-pane -h -s :+
bind b break-pane -a

# Toggle the per-pane title bar (pane-border-format) on the top border
bind -n M-t if -F "#{==:#{pane-border-status},off}" \
  "set -w pane-border-status top" \
  "set -w pane-border-status off"

# Restore ctrl+h/j/k/l keys (taken by smart-splits) by pressing the prefix key first
bind C-h send-keys "C-h"
bind C-j send-keys "C-j"
bind C-k send-keys "C-k"
bind C-l send-keys "C-l"

# Window
bind -n M-1 select-layout even-horizontal
bind -n M-2 select-layout even-vertical
bind -n M-3 select-layout main-horizontal
bind -n M-4 select-layout main-vertical
bind -n M-5 select-layout tiled
bind -n M-Enter resize-pane -Z
bind -n M-, swap-window -t -1 \; previous-window
bind -n M-. swap-window -t +1 \; next-window
# Windows of this session, most recently used first; the selection follows
bind w run-shell "~/.config/tmux/scripts/window-picker.sh"
bind W choose-tree -wZy

# Send this window (or only its out-of-place panes) to the session its working
# directory belongs to; M gathers every pane in this session sharing the
# current pane's directory. --all (unbound) sorts the whole session.
bind m run-shell "~/.config/tmux/scripts/window-sort.sh '#{pane_id}'"
bind M run-shell "~/.config/tmux/scripts/window-sort.sh --session '#{pane_id}'"
bind c new-window -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"
bind s split-window -v -c "#{pane_current_path}"

# Session
bind o run-shell "~/.config/tmux/scripts/session-picker.sh"
bind L run-shell "~/.config/tmux/scripts/session-picker.sh --last"

# Claude Code
bind . run-shell "~/.config/tmux/scripts/claude-pane.sh '#{pane_current_path}'"
bind > new-window -c "#{pane_current_path}" "fish -lc 'exec claude'"
bind / run-shell "~/.config/tmux/scripts/claude-picker.sh --current --client '#{client_name}'"
bind ? run-shell "~/.config/tmux/scripts/claude-picker.sh --all --client '#{client_name}'"

# --- Popups ---
# Inside any popup: M-Enter toggles default size <-> full screen; its own key
# closes it; any other popup key here switches straight to that popup.
bind -n M-/ run-shell "~/.config/tmux/scripts/popup-toggle.sh -s session"
bind -n M-g run-shell "~/.config/tmux/scripts/popup-toggle.sh -s project -n lazygit lazygit"
bind -n M-m run-shell "~/.config/tmux/scripts/popup-toggle.sh -s server -n btop btop"
bind -n M-n run-shell "~/.config/tmux/scripts/popup-toggle.sh -s server -n nvtop nvtop"
bind -n M-d run-shell "~/.config/tmux/scripts/popup-toggle.sh -s project -n hunk hunk diff"
bind -n M-r run-shell "~/.config/tmux/scripts/popup-toggle.sh -s project -n tuicr tuicr"
# Push the binds above to a popup server that's already running (no-op if it
# isn't — a fresh one mirrors them itself on the first popup open).
run-shell -b "~/.config/tmux/scripts/popup-toggle.sh --mirror"

# Kill popup sessions whose owner pane/window/session is gone
set-hook -g pane-exited[42] 'run-shell "~/.config/tmux/scripts/popup-toggle.sh --gc"'
set-hook -g window-unlinked[42] 'run-shell "~/.config/tmux/scripts/popup-toggle.sh --gc"'
set-hook -g session-closed[42] 'run-shell "~/.config/tmux/scripts/popup-toggle.sh --gc"'

# --- Theme ---
source-file "~/.config/tmux/tmux.theme.conf"

# --- Plugins (TPM) ---
set -g @plugin "tmux-plugins/tpm"
set -g @plugin "tmux-plugins/tmux-sensible"
set -g @plugin "tmux-plugins/tmux-resurrect"
set -g @plugin "tmux-plugins/tmux-continuum"
set -g @plugin "tmux-plugins/tmux-logging"
set -g @plugin "mrjones2014/smart-splits.nvim"

# Bootstrap TPM on first run
if "test ! -d ~/.config/tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins/tpm && ~/.config/tmux/plugins/tpm/bin/install_plugins'"

run "~/.config/tmux/plugins/tpm/tpm"
